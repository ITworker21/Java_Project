<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">

 <select id="getSetmealIdsByDishId" resultType="java.lang.Long">
  select setmeal_id from setmeal_dish where dish_id in 
    <foreach collection="dishIds" item="dishId" separator="," close=")" open="(">
     #{dishId}
    </foreach>
 </select>

    <insert id="insertBatch">
        insert into setmeal_dish (setmeal_id, name,dish_id,price,copies) value
        <foreach collection="setmealDishList" item="df" separator=",">
            (#{df.setmealId},#{df.name},#{df.dish_id},#{df.price},#{df.copies})
        </foreach>
    </insert>

</mapper>
